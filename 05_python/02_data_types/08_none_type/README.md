# None Type (`None`) 🚫

---

## What is `None`?

`None` represents the **absence of a value**.

Key points:
- type of `None` is `NoneType`
- many functions return `None` when they don’t explicitly return anything
- recommended check: `x is None` (not `x == None`)

---

## Files in This Folder 📁

| File | Focus |
|------|-------|
| `none_type.py` | None basics, comparisons, common patterns |
| `exercises.md` | practice |

---
