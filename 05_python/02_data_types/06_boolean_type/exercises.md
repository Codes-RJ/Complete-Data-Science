# Exercises — Boolean Type (`bool`) 📝

---

## A. Predict the Output

1.

```python
print(bool(0))
print(bool("0"))
print(bool([]))
print(bool([0]))
```

2.

```python
print(True + True + False)
print(int(True), int(False))
```

---

## B. Comparisons

For each expression, write `True` or `False`:

1. `10 > 5`
2. `10 == 10`
3. `7 != 8`
4. `5 >= 5`
5. `3 < 2`

---

## C. Logical operators

Evaluate:

1. `True and False`
2. `True or False`
3. `not True`
4. `(5 > 2) and (3 < 1)`
5. `(5 > 2) or (3 < 1)`

---

## D. Small Programs

1. Ask a user for a number and print if it is:
   - positive
   - negative
   - zero
2. Ask a user for age and print if they are eligible to vote (age >= 18).

---

## Challenge ⭐

Write a program that:
- asks the user for a password string
- prints `Strong` if:
  - length >= 8
  - has at least one digit
  - has at least one uppercase letter
Otherwise print `Weak`

